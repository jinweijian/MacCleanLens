# Changelog

## 0.1.2

- 新增 `mac-clean-lens-update` 命令，用户可用一条命令更新 npm 包并重新安装 macOS App。
- 当全局 npm 安装目录需要管理员权限时，更新命令会弹出 macOS 授权窗口，不再要求用户手动输入 `sudo npm install -g mac-clean-lens@latest`。
- 构建与安装流程增加持续的 doing 进度动效，每个步骤都会保持刷新，减少等待时的卡顿感。

## 0.1.1

- 增加深度扫描、权限提示、Finder/系统设置快捷操作等能力。
- 将 Node.js 运行时打包进 macOS App，降低用户本机环境依赖。
- 改进启动台 App 图标和安装体验。

## 0.1.0

- 首次发布。
- 支持扫描常见缓存、日志、下载文件、大文件和项目依赖。
- 支持将用户勾选的可清理项目移动到废纸篓。
